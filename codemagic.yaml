# iOS builds without a Mac: Codemagic (free tier: 500 macOS build minutes/month).
#
# One-time setup: DONE 2026-07-27. Apple Developer enrolled, ASC API key
# created (App Manager role), Codemagic integration named masar-asc-key,
# bundle id app.masar.talib registered, App Store Connect app record created
# (app id 6795260984). Nothing here needs redoing -- just start the workflow.
#
# Monorepo note: this file MUST stay at the repository root (Codemagic only
# looks for codemagic.yaml there); `working_directory: app` is what targets
# the Flutter app. Codemagic's onboarding scanner will still complain that the
# root has no mobile app -- ignore it, or set Project path to `app`, it does
# not affect the yaml build.
#
# To add a tester: App Store Connect → TestFlight → Internal Testing. Internal
# testers skip Beta App Review; external testers do not.

workflows:
  ios-testflight:
    name: iOS → TestFlight
    max_build_duration: 45
    instance_type: mac_mini_m2
    environment:
      flutter: 3.44.6
      xcode: latest
      cocoapods: default
      # NOT `integrations: app_store_connect: masar-asc-key` -- that lookup
      # failed with "integration does not exist" even with the key present and
      # valid under Developer Portal → Manage keys. Passing the key as plain
      # env vars skips the lookup; the CLI reads these names automatically.
      groups:
        - appstore_credentials
      vars:
        BUNDLE_ID: app.masar.talib
    working_directory: app
    scripts:
      - name: Get packages
        script: flutter pub get
      - name: Run tests
        script: flutter test
      - name: Set up signing from the App Store Connect integration
        # NOT the `ios_signing:` environment block -- that only FETCHES existing
        # signing files and fails preparation with "No matching profiles found"
        # before any script runs. --create provisions the distribution cert and
        # the app_store profile on first build, then reuses them after.
        script: |
          keychain initialize
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Build signed IPA
        script: |
          flutter build ipa --release \
            --build-number=$PROJECT_BUILD_NUMBER \
            --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
